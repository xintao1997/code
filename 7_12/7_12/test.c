#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include<stdlib.h>
//给定两个整形变量的值，将两个值的内容进行交换。
/*int main()
{
	int a = 1;
	int b = 2;
	int c = 0;
	printf("a=%d,b=%d\n", a, b);
	c = a;
	a = b;
	b = c;
	printf("a=%d,b=%d\n", a, b);
	system("pause");
	return 0;
}*/
//不允许创建临时变量，交换两个数的内容（附加题）